<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Mac 端专用。跟 iOS 那份（PendingBot.entitlements）逐键的差异只有一条：
       **这里没有推送**。

       为什么要分家（2026-08-20 实测，不是照抄别人的经验）：
       共用那份里的 `aps-environment` 是 **iOS 专有键**，macOS 对应的是
       `com.apple.developer.aps-environment`（两个不同的键）。拿 iOS 那份去
       archive macOS，Xcode 会把它**静默剥掉** —— 不报错、照样签、照样能公证，
       产物里就是没有。发版脚本里那道「源里声明的每个键，产物里必须真的还在」
       的断言，因此会永远红着，于是那道断言的全部价值（键被吞的时候会响）就
       没了。

       为什么是「去掉」而不是「换成 macOS 版的推送键」：Mac 端根本没有推送
       代码 —— `Sources/Networking/PushService.swift` 整个文件包在
       `#if os(iOS)` 里。声明一个用不上的能力只会让产物多带一条 entitlement。
       哪天 Mac 真要推送，往这儿加 `com.apple.developer.aps-environment`
       （Developer ID 描述文件里已经授权了这个键，值是 production）。 -->
  <!-- **这里没有 `com.apple.developer.applesignin`，是被 Apple 挡住的，不是漏了。**

       Apple 不给「直接分发」(Developer ID，非 Mac App Store) 的 macOS 应用签发
       这项授权。DTS 原话见 <https://developer.apple.com/forums/thread/129263>：
       该能力不支持 Developer ID；有人为此提 bug，Apple 回的是「此行为符合预期」。

       2026-08-21 在本项目账号上实测坐实：把发布用的描述文件 PendingBotDistribute
       **重新生成**了一张（新 UUID、当天的生成时刻），该项授权仍然不在文件里；而同
       一个 App ID 的**开发**描述文件里有。所以不是文件太旧。

       写在这儿要么被静默剥掉、要么让 exportArchive 直接失败 —— 2026-08-07 那版
       就是被静默剥掉的：登录页上 Apple 按钮点了没用，而签名有效、公证通过、
       Gatekeeper 放行，全链路零报错，两周没人发现。

       配套改动：Mac 登录页那颗 Apple 按钮改成按「包实际拿到的授权」显示
       （`Sources/Mac/MacWelcomeView.swift` 的 appleSignInAvailable）。**这两件事
       必须一起，只删这里等于把死按钮留在界面上。**

       **什么时候把它加回来**：改走 TestFlight / Mac App Store 分发的时候。那条路
       的描述文件带这项授权，按钮也会自己回来（判定是运行时读授权，不是写死平台）。

       iOS 那份（PendingBot.entitlements）不受影响，照常声明。 -->
  <key>com.apple.developer.associated-domains</key>
  <array>
    <string>applinks:bot.pendingname.com</string>
  </array>
  <key>keychain-access-groups</key>
  <array>
    <string>$(AppIdentifierPrefix)com.pendingname.pendingbot</string>
    <!-- 家族 SSO 共享组：与 PendingCrew 同组，双方经 kSecAttrAccessGroup 显式
         读写家族凭据(pfa_)。keychain group 无需 portal 配置(profile 自动允许
         TEAMID.* 下所有组)。spec: 2026-06-08-pendingcrew-login-sso-design.md -->
    <string>$(AppIdentifierPrefix)com.pendingname.shared</string>
  </array>
</dict>
</plist>
