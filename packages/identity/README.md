# @pendingbot/identity

Cross-product identity package. Verifies Supabase Auth JWTs and provides Hono middleware (`requireSession`, `requireAdmin`) for any product app that authenticates against the same Supabase project.

Used by `apps/edge/` and any future PendingBot products.
