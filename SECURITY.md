# 安全策略

## 怎么报告

**请不要用公开 issue 报告安全问题。**

用 GitHub 的私有漏洞报告通道：

👉 **[Security → Advisories → Report a vulnerability](../../security/advisories/new)**

（也可以从仓库页面顶部的 **Security** 标签进去，点 "Report a vulnerability"。）

这条通道只有仓库所有者看得到，可以在里面贴复现步骤、日志、补丁，
修完之后再一起公开。这里不留邮箱——GitHub 这条通道是唯一的。

## 请一并说明

- 受影响的组件：Edge Worker / Swift 客户端 / 数据库迁移 / 后台控制台 / 构建脚本
- 复现步骤，越具体越好
- 影响：能读到什么、能写什么、能冒充谁
- 你测的是哪个 commit

## 你能期待什么

**说在前面：这是一个人的业余项目，没有值班，没有响应时限。**
作者会尽量在几天内回一句"收到"，但请不要按商业产品的响应速度预期。

如果你两周没收到任何回复，可以在 advisory 里再顶一次。

## 什么算范围内

这个仓库里的代码：Edge Worker、Swift 客户端、数据库迁移与 RLS/权限、
后台控制台、构建与发版脚本。

尤其想知道的是这几类：

- 未鉴权就能读到或写入他人数据的路径（RLS 或 `SECURITY DEFINER` 的洞）
- 鉴权中间件能被绕过
- 客户端里能被提权成服务端权限的东西
- 供应链问题：构建脚本、CI workflow、vendored 的依赖

## 什么算范围外

- **对作者部署的实例做测试。** 别打线上，本地起一套自己的（见 [`docs/self-hosting.md`](docs/self-hosting.md)）。
- 仓库里那些形如 `YOUR_…` / `example.com` 的占位值——它们不是泄漏的凭据，是故意的占位。
- 已经在 README 里明说"没接通/没验证"的功能里的问题（那些是已知的半成品，不是漏洞）。
- 需要物理接触设备、或需要用户先自己装个恶意描述文件之类的场景。
- 第三方服务（Supabase / Cloudflare / Apple 等）本身的问题——请报给他们。

## 已知且不打算修的

`docs/self-hosting.md` 和 README 的「状态」一节列了当前明确未接通、
未验证、或需要额外凭据才动得了的功能。那些是已知缺口，报了也只会得到
"是的，知道"。真正的漏洞报告请聚焦在**看起来应该是安全的、但其实不是**的地方。

## 支持范围

只有 `main` 的最新提交。没有版本分支，没有向后移植。
