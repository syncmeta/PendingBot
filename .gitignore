# Dependencies
node_modules/
**/node_modules/

# Build output
**/dist/
**/.build/
**/.vite/
**/.next/
**/.vercel/
**/.turbo/
**/next-env.d.ts
**/tsconfig.tsbuildinfo

# scroll-lab experiment run outputs (scratch)
apps/scroll-lab/runs/

# SQLite database files (only matter for self-host dev runs)
**/data/*.sqlite
**/data/*.sqlite-shm
**/data/*.sqlite-wal
**/data/uploads/

# Environment files
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local

# macOS
.DS_Store

# Xcode user-specific state
**/xcuserdata/
*.xcuserstate

# Xcode 本地生成物（.xcodeproj 本身是入库的，由 project.yml + xcodegen 生成；
# scheme 和用户状态不入库，所以新 clone 要先跑一次 xcodegen）
apps/pendingbot/PendingBot.xcodeproj/xcuserdata/
apps/pendingbot/PendingBot.xcodeproj/xcshareddata/
apps/pendingbot/build/
**/DerivedData/

# 发版产物
dist-release/
*.dmg
*.ipa
*.xcarchive

# 老 iOS 工程残留(T0.1 把 apps/ios/ 迁移到 apps/pendingbot/ 后,build/ 里仍有
# 老 TestFlight archive(.xcarchive + .ipa)— 保留供历史回溯,但不入 git。
apps/ios/

# 编码 agent 的本地配置（各人自便，不入 git）
# 注意：CLAUDE.md / AGENTS.md 在**私有仓里是被跟踪的**，gitignore 对已跟踪文件
# 无效，所以这两行在这儿是空转的。它们真正生效的地方是**公开快照** ——
# 快照是拷贝不是 clone，这两行是「别让内部约定跟着出去」的最后一道兜底。
# 删掉它们在私有仓里看不出任何区别，在公开仓里少一层保护。
.claude/
.codex/
.superpowers/
.agents/
.pendingcrew/
AGENTS.md
CLAUDE.md
GEMINI.md

# Deploy script (contains prod infra defaults)
deploy.sh
